@page
@model Proyect3TWN.WebSite.Pages.Users.Edit

<div class="container-fluid">
    <div class="row">
        <div class="col-md-1"></div>
        <div class="col-md-10">
            <h2>Editing</h2>
            <form method="post">
                <input type="hidden" asp-for="UsersDto.id"/>
                <div class="form-group">
                    <label asp-for="UsersDto.UserName"></label>
                    <input asp-for="UsersDto.UserName" class="form-control" maxlength="45"/>
                    <span class="text-danger" asp-validation-for="UsersDto.UserName"></span>
                </div>
                <div class="form-group">
                    <label asp-for="UsersDto.Email"></label>
                    <input asp-for="UsersDto.Email" class="form-control" maxlength="45"/>
                    <span class="text-danger" asp-validation-for="UsersDto.Email"></span>
                </div>
                <div class="form-group">
                    <label asp-for="UsersDto.Password"></label>
                    <input asp-for="UsersDto.Password" class="form-control" maxlength="45"/>
                    <span class="text-danger" asp-validation-for="UsersDto.Password"></span>
                </div>
                
                @if (Model.Errors.Count > 0)
                {
                
                    <div class="alert alert-danger" role="alert">
                        <ul>
                            @foreach (var error in Model.Errors)
                            {
                                <li>An error</li>
                            }
                        </ul>
                    </div>
                }
                <div class="row">
                    <div class="col-md-12 text-right">
                        <button type="submit" class="btn btn-primary mr-3">Save</button>
                        <a class="btn btn-secondary"> Cancel </a>
                    </div>
                </div>
            </form>
        </div>
        <div class="col-md-1"></div>
    </div>
</div>